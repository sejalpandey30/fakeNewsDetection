import sys
print("Step 1: start", flush=True)

import numpy
print("Step 2: numpy", flush=True)

import pandas
print("Step 3: pandas", flush=True)

import textstat
print("Step 4: textstat", flush=True)

from textblob import TextBlob
print("Step 5: textblob", flush=True)

import torch
print("Step 6: torch", flush=True)

import sklearn
print("Step 7: sklearn", flush=True)

import lime
print("Step 8: lime", flush=True)

print("All imports successful!", flush=True)
